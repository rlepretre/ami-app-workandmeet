import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'be.workandmeet.app',
  appName: 'app-workandmeet',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
